#!/bin/bash
# pre-push 门控钩子：推送 main 前强制 make gate
set -e
while read local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "==> pushing main: running gate"
    make gate
  fi
done
